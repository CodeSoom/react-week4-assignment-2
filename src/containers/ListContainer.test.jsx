describe('ListContainer', () => {
  context('등록된 식당이 있을 때,', () => {
    it('식당을 출력한다.', () => {});
  });

  context('등록된 식당이 없을 때,', () => {
    it('등록된 식당이 없어도 정상적인 화면을 출력한다.', () => {});
  });
});
