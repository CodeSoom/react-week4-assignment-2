const initialState = {
  inputTitles: {
    name: '',
    category: '',
    address: '',
  },
  list: [],
};

export default function reducer(state = initialState, action = { type: '' }) {
  return state;
}
