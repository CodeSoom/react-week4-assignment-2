/**
 * @param state {object}
 * @param action {object}
 * @returns {undefined}
 */
const initState = {
  name: '',
  category: '',
  address: '',
};

export default function reducer(state = initState, action) {
  return state;
}
