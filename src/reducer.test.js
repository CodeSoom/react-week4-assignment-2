import reducer from './reducer';

import { updateRestaurant, addRestaurant } from './actions';

import restaurantList from '../fixtures/restaurantList';

describe('reducer', () => {
  describe('updateRestaurant', () => {
    it('입력한 레스토랑을 업데이트 해준다', () => {});
  });
  describe('addRestaurant', () => {
    it('입력한 레스토랑을 추가해준다', () => {});
  });
});
